# 🎯 دليل الاستخدام البسيط جداً

## ⚠️ المشكلة عندك الآن:

**EA غير موجود!** 

EA = Expert Advisor (البرنامج على MT5 اللي يقرأ الإشارات وينفذ الصفقات)

---

## 📋 الخطوات بالتفصيل الممل:

### خطوة 1: افحص النظام

```bash
CHECK_SYSTEM.bat
```

هذا الملف **يفحص كل شيء** ويقول لك إيه الناقص.

**المفروض تشوف:**
```
[OK] Python installed
[OK] Python libraries installed
[OK] MT5 found
[ERROR] EA NOT found!  ← المشكلة هنا
```

---

### خطوة 2: فهم البنية

```
Python (يولد الإشارات)
    ↓
bridge.txt (ملف نصي)
    ↓
EA على MT5 (يقرأ ملف bridge.txt وينفذ الصفقات)  ← هذا الناقص!
    ↓
صفقات على MT5
```

**بدون EA = لا توجد صفقات!**

---

## 🔴 الحل: تحتاج ملف EA

### أنت تحتاج واحد من هذين:

#### Option 1: الملف المترجم (.ex5)
```
EA_SignalBridge.ex5
```
- جاهز للاستخدام
- ضعه في مجلد Experts في MT5

#### Option 2: الملف المصدر (.mq5)
```
EA_SignalBridge.mq5
```
- كود MQL5 نصي
- يحتاج ترجمة في MetaEditor

---

## 📂 أين تضع EA؟

### الطريقة السهلة:

1. **افتح MT5**
2. اضغط **File → Open Data Folder**
3. افتح مجلد **MQL5 → Experts**
4. **انسخ EA_SignalBridge.ex5 هنا**
5. **أعد تشغيل MT5**

### المسار الكامل:
```
C:\Users\[YourName]\AppData\Roaming\MetaQuotes\Terminal\[TERMINAL_ID]\MQL5\Experts\EA_SignalBridge.ex5
```

---

## 🎬 كيف يعمل النظام (بالتفصيل):

### 1. توليد الإشارات (Python)

```bash
RUN_BACKTEST.bat
```

**ماذا يحدث داخلياً:**
```bash
# 1. يشغل Python
python bridge\generate_signals.py

# 2. Python يحلل السوق حسب SMC
#    - يكشف Sweeps
#    - يكشف CHoCH
#    - يكشف OB/FVG
#    - يكون إشارات

# 3. يكتب ملفين:
signals\bridge.txt          # الإشارات
signals\drawings.json       # معلومات الرسم

# 4. ينسخهم لـ MT5
copy signals\bridge.txt %APPDATA%\MetaQuotes\Terminal\Common\Files\
copy signals\drawings.json %APPDATA%\MetaQuotes\Terminal\Common\Files\

# 5. يفتح MT5 Strategy Tester
```

### 2. EA يقرأ الإشارات (داخل MT5)

**عند تشغيل Strategy Tester:**

```
1. MT5 يبدأ الباكتيست
2. EA يقرأ bridge.txt من Common\Files
3. EA يقرأ كل سطر:
   timestamp;symbol;action;price;sl;tp;risk;comment
4. EA يرسم على الشارت (إذا drawings.json موجود)
5. EA ينفذ الصفقة:
   - BUY at price
   - SL at sl
   - TP at tp
6. MT5 يسجل النتائج
```

**بدون EA = الخطوات 2-6 مش بتحصل!**

---

## 🔍 تشخيص المشكلة عندك:

### السيناريو 1: MT5 يفتح Strategy Tester بس مفيش EA

**الأعراض:**
- MT5 يفتح ✅
- Strategy Tester مفتوح ✅
- بس مفيش EA في القائمة ❌

**السبب:**
EA مش موجود في مجلد Experts

**الحل:**
احصل على EA_SignalBridge.ex5 وضعه في المجلد الصحيح

---

### السيناريو 2: EA موجود بس مفيش صفقات

**الأعراض:**
- EA في القائمة ✅
- باكتيست يشتغل ✅
- بس مفيش صفقات ❌

**الأسباب المحتملة:**

#### أ) EA مش بيقرأ الملف
```
تحقق من Inputs في Strategy Tester:
InpEnableTrading = true      ← لازم true
InpSource = 0                ← لازم 0 (Common Files)
InpFileOrMask = "bridge.txt" ← لازم bridge.txt
```

#### ب) bridge.txt فاضي أو مش موجود
```bash
# افحص الملف
notepad signals\bridge.txt

# لازم يكون فيه سطور زي:
timestamp;symbol;action;price;sl;tp;risk;comment
2025-11-21T10:00:00Z;EURUSD;BUY;1.08500;...
```

#### ج) الإشارات مولدة بس مش منسوخة
```bash
# تأكد الملف موجود في MT5:
dir "%APPDATA%\MetaQuotes\Terminal\Common\Files\bridge.txt"
```

---

## 📝 خطوات الاستخدام الصحيحة:

### المرة الأولى (Setup):

```bash
# 1. افحص النظام
CHECK_SYSTEM.bat

# 2. اصلح أي أخطاء
#    - نصّب Python إذا ناقص
#    - نصّب المكتبات: pip install -r requirements.txt
#    - نصّب MT5 إذا ناقص

# 3. احصل على EA
#    - EA_SignalBridge.ex5
#    - ضعه في: MT5 Data Folder\MQL5\Experts\

# 4. افحص مرة ثانية
CHECK_SYSTEM.bat
#    لازم تشوف: [OK] كل شيء

# 5. أعد تشغيل MT5
```

### كل مرة بعد كده:

```bash
# 1. شغّل الباكتيست
RUN_BACKTEST.bat

# 2. MT5 سيفتح Strategy Tester

# 3. تحقق من الإعدادات:
#    - Expert: EA_SignalBridge
#    - Symbol: EURUSD (أو حسب config.yaml)
#    - Period: M5 (أو حسب config.yaml)
#    - Inputs:
#      * InpEnableTrading = true
#      * InpSource = 0
#      * InpFileOrMask = "bridge.txt"

# 4. اضغط Start

# 5. انتظر نهاية الباكتيست

# 6. راجع النتائج في Results tab
```

---

## 🎥 فيديو الخطوات (نصي):

```
خطوة 1: افتح CMD كـ Administrator
├─ Win + X → Command Prompt (Admin)

خطوة 2: انتقل للمشروع
├─ cd C:\AI-Trading-System

خطوة 3: افحص النظام
├─ CHECK_SYSTEM.bat
├─ اقرأ النتيجة بعناية
└─ لو فيه [ERROR] اصلحه

خطوة 4: ضع EA في مكانه
├─ افتح MT5
├─ File → Open Data Folder
├─ MQL5 → Experts
├─ copy EA_SignalBridge.ex5 here
└─ أعد تشغيل MT5

خطوة 5: ولّد الإشارات
├─ RUN_BACKTEST.bat
├─ ينتظر...
└─ MT5 سيفتح

خطوة 6: في MT5 Strategy Tester
├─ Expert: EA_SignalBridge
├─ Symbol: EURUSD
├─ Period: M5
├─ Inputs: تحقق من الإعدادات
├─ اضغط Start
└─ شوف النتائج
```

---

## 🔴 المشكلة الرئيسية عندك:

### 99% المشكلة هي:

**EA مش موجود على جهازك!**

**الحل:**
1. احصل على `EA_SignalBridge.ex5` من المطور
2. ضعه في: `MT5 Data Folder\MQL5\Experts\`
3. أعد تشغيل MT5
4. جرب تاني

---

## 📞 لو محتاج EA:

### أنت محتاج المطور يعطيك:

**Option 1 (الأسهل):**
```
EA_SignalBridge.ex5
```
ملف جاهز، ضعه وشغّل

**Option 2 (للمطورين):**
```
EA_SignalBridge.mq5
```
كود MQL5، تفتحه في MetaEditor وتترجمه

---

## 📊 ملفات المشروع (للفهم):

```
C:\AI-Trading-System\
│
├── bridge/
│   └── generate_signals.py     ← Python يولد الإشارات
│
├── signals/
│   ├── bridge.txt               ← الإشارات (يقرأها EA)
│   └── drawings.json            ← معلومات الرسم (يقرأها EA)
│
├── automation/
│   ├── RUN_BACKTEST.bat         ← شغّل هذا
│   ├── run_tester.bat           ← يفتح MT5
│   └── tester.ini               ← إعدادات MT5
│
├── strategy/
│   ├── strategy.py              ← استراتيجية SMC
│   └── config.yaml              ← إعدادات
│
└── CHECK_SYSTEM.bat             ← افحص النظام
```

**في MT5:**
```
%APPDATA%\MetaQuotes\Terminal\
│
├── Common\Files\
│   ├── bridge.txt               ← EA يقرأ من هنا
│   └── drawings.json            ← EA يقرأ من هنا
│
└── [TERMINAL_ID]\MQL5\Experts\
    └── EA_SignalBridge.ex5      ← EA لازم يكون هنا!
```

---

## ✅ Checklist:

قبل ما تشغّل باكتيست، تأكد:

- [ ] Python مثبت
- [ ] المكتبات مثبتة (pip install -r requirements.txt)
- [ ] MT5 مثبت
- [ ] **EA موجود في Experts folder** ← الأهم!
- [ ] ملفات الإشارات موجودة (signals/bridge.txt)
- [ ] الملفات منسوخة لـ MT5 Common Files

**كل [ ] لازم يكون [x]!**

---

## 🆘 لو لسه مش فاهم:

**شغّل:**
```bash
CHECK_SYSTEM.bat
```

**وابعت لي النتيجة!**

سأعرف بالضبط إيه المشكلة وأحلها لك.

---

**الخلاصة في سطر واحد:**

**Python بيولد إشارات → EA على MT5 بيقرأها وينفذها → EA ناقص عندك!**

🔴 **احصل على EA_SignalBridge.ex5 أولاً!** 🔴
